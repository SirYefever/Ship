<Window x:Class="Ship.CrewerRedactWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ship"
        mc:Ignorable="d"
        Title="CrewerRedactWindow" Height="150" Width="300">
    <StackPanel Background="Bisque">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" >Имя: </TextBlock>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name, Mode=TwoWay}"></TextBox>
            <TextBlock Grid.Row="1" Grid.Column="0">Пол: </TextBlock>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Sex, Mode=TwoWay}"></TextBox>
        </Grid>
    </StackPanel>
</Window>
