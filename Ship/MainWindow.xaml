<Window x:Name="window" x:Class="Ship.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ship"
        mc:Ignorable="d"
        Title="MainWindow" Height="1080" Width="1920" Background="{DynamicResource backgroundImage}">
    <Window.Resources>

        <ImageSource x:Key="imgSource">background.png</ImageSource>
        <ImageBrush x:Key="backgroundImage" ImageSource="{DynamicResource imgSource}" Opacity="0.3"></ImageBrush>

    </Window.Resources>
    <Grid x:Name="MAIN">
        <Grid.RowDefinitions>
            <RowDefinition Height="72*"/>
            <RowDefinition Height="1008*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" HorizontalAlignment="Center" FontSize="32" FontWeight="SemiBold" Padding="10">
        <Run Foreground="Cyan" Text="Паблик-Кораблик"/>
        </TextBlock>
        <Grid Grid.Row="1" x:Name="SplitInTwo">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="555*"/>
                <ColumnDefinition Width="1365*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Border BorderBrush="White" BorderThickness="2" Margin="0 0 4 0" Grid.Column="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition Height="79.2"/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Button Grid.Column="4" Grid.Row="1" />
                </Grid>
            </Border>
        </Grid>
    </Grid>

</Window>
